#include <stdio.h>
int main()
{
    int num;
    int count = 0;
    printf("Enter an number is: ");
    scanf("%d", &num);
    while(num != 0)
    {
        num /= 10;
        ++count;
    }

    printf("Number of digits: %d", count);
}
